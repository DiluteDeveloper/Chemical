engine-dev 1.42 WIP

changelog:

scene composition/inheritance hierarchy system

todo:

supply transform from 3d model loading to actual transform
load entire model files as scenes, currently only loads first mesh

I think for what i need, a scene hierachy, i need a base object to inherit from for generic objects, then past that composition can be used